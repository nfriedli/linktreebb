<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <style>
        body {
            max-width: 30rem;
            margin: auto;
            padding: 1rem;
        }
        ul.buttons {
            display: flex;
            flex-direction: column;
            list-style-type: none;
            margin: 0;
            padding: 0;
            gap: 1rem;
            text-align: center;
        }

        .buttons li {
            border: 1px solid red;
            padding: 1rem;
        }

        .buttons a {
            color: inherit;
            text-decoration: none;
            display: block;
        }
    </style>
</head>
<body>

    <h1>{{ title or page.fileSlug }}</h1>
    {% if url %}
        Redirection (à automatiser) vers: <a href="{{url}}">{{url}}</a>
    {% elif links %}
        <ul class="buttons">
            {% for l in links %}
                <li><a href="{{ l.url }}">{{ l.title }}</a></li>
            {% endfor %}
        </ul>
    {% else %}
        {{ content | safe }}
    {% endif %}

</body>
</html>